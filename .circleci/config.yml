version: 2.1
orbs:
  ruby: circleci/ruby@1.1.3

jobs:
  build:
    docker:
      - image: circleci/ruby:2.5.3
    executor: ruby/default
    steps:
      - checkout
      # - bundler-version: '2.1.4'
      - ruby/install-deps
      - run: rails db:setup
  test:
    docker:
      - image: circleci/ruby:2.5.3
    executor: ruby/default
    steps:
      - ruby/rspec-test

workflows:
  version: 1
  build_and_test:
    jobs:
      - build
      - test:
        requires:
          - build
